// Agente Atacante

/* Initial beliefs and rules */

posicao(5, 8).
time(team_a).
		

/* Initial goals */

// objetivo inicial apenas entrar em campo
!entrarEmCampo.

/* Plans */

// ao entrar em campo, obter a posicao inicial do jogador em campo e o time,
// criar o jogador no tewnta e comecar a rastrear a bola.
+!entrarEmCampo : true
    <-  ?posicao(X,Y); ?time(Z);
       createPlayer(X, Y, Z);
       !olheBola.
	   

//ao inserir o objetivo olheBola na base de conhecimentos, sob qualquer condicao
// (sempre true), resgate a posicao da bola (percepcao) e rotacione olhando pra bola
+!olheBola : true
	<- ?posBola(X,Y);
		rotacioneParaBola(X,Y);
		!buscaBola;
		!olheBola.
		
		
+!buscaBola : true
	<- ?posBola(X,Y);
		irLinhaReta(X,Y);
		!buscaBola.




